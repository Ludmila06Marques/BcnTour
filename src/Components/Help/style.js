import styled from "styled-components";

export const HelpCotainer=styled.div`
padding: 10px;

`

export const Title=styled.h1`
font-size: 30px;
font-weight: bold;
color: ${({theme})=> theme.colorContainer};

`